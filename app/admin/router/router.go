package router

import (
	"github.com/gin-gonic/gin"
)

func Load(r *gin.RouterGroup) {

	// userGroup := r.Group("/user")
	// userGroup.GET("/:id", middleware.FrontendAuthMiddleware(), frontend.GetUser)
	// userGroup.POST("/", middleware.FrontendAuthMiddleware(), frontend.CreateUser)
	// 添加更多前台用户路由

}
